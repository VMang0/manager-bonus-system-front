import { styled } from '@mui/system';
import { Box } from '@mui/material';

export const AvatarsGroupStyled = styled(Box)(() => ({
  '.MuiAvatar-root': {
    width: '37px',
    height: '37px',
  },
}));
