import { styled } from '@mui/system';
import { Box } from '@mui/material';

export const UsersForActivateStyled = styled(Box)(() => ({
  display: 'flex',
  justifyContent: 'space-between',
  padding: '0 15px',
  '.UserInfoForm': {
    width: '48%',
  },
}));
